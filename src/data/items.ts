import type { Item } from './types'

/**
 * Data demo dikosongkan — aplikasi kini mengambil item dari database (Neon)
 * lewat /api/bootstrap. Array ini hanya fallback saat backend belum siap.
 */
export const ITEMS: Item[] = []
